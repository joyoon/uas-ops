services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: uas_ops
      POSTGRES_USER: uas_ops
      POSTGRES_PASSWORD: uas_ops_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/schema.sql:/docker-entrypoint-initdb.d/schema.sql

volumes:
  pgdata:
